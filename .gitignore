# Claude Code local settings
.claude/

# Hugo build output & generated resource cache
/public/
/resources/_gen/
/.hugo_build.lock
hugo_stats.json
.hugolock

# Python virtualenv
.venv/

# Local RAG tooling (contains a Dify API key — keep out of version control)
upload_to_dify.py

# macOS
.DS_Store
